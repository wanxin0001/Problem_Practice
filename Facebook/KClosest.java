/* 
Find k closest number in an array of a given value
*/
import java.util.*;

public class KClosest {
	public ArrayList<Integer> method (int[] array, int target, int k) {
		if (array == null || array.length == 0) {
			return null;
		}

		PriorityQueue<Integer> priQueue = new PriorityQueue<Integer>()
	}
}